<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URl Shortner</title>
</head>

<body>

    <h1>Welcome To Url Shortner</h1>

    <div class="box">
        <form action="/url" method="post">
            <label for="url">Please Enter Your Url</label>
            <input type="url" id="url" name="url">
            <button type="submit">Go</button>
        </form>
        <% if(customId!=null && customId!=undefined){%>
            <h1>Your Custom url: <a href="http://localhost:4000/url/<%=customId%>">https://localhost:4000/url/<%=customId%></h1></a> 
            <%  }%>
    </div>

    <div class="old">
        <h1>Your previous generated Urls:</h1>
        <% if(urls!=undefined && urls!=null){ %>
            <% for(let url of urls){ %>
                <span><%= url.redirect_url %></span>
                <span><a href="http://localhost:4000/url/<%=url.short_url%>">http://localhost:4000/url/<%=url.short_url%></a></span>
                <br>
                <% }%>
            <% }%>
    </div>

</body>

</html>